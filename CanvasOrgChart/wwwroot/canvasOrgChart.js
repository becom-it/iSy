window.canvasOrgChartJSInterop = {
    Initializie: canvasId => {
        let canv = document.getElementById(canvasId);
        if (canv !== null) {
            console.log("Canvas found!");
        }
    }
}